#include <iostream>
using namespace std;

int main() {
    int x = 4;

    cout << "Original value = " << x << endl;
    cout << "Left Shift (x << 1) = " << (x << 1) << endl;
    cout << "Right Shift (x >> 1) = " << (x >> 1) << endl;

    return 0;
}